<p style="color: green"><%= notice %></p>

<% content_for :title, "La boîte à..." %>

<div class="items-center justify-center w-full text-center my-8">
  <h1 class="mb-4 text-4xl font-extrabold leading-none tracking-tight text-gray-900 md:text-5xl lg:text-6xl dark:text-white">
    La boîte à...
  </h1>
</div>

<div id="people" class="flex items-center justify-center flex-col md:flex-row md:flex-wrap">
  <% @people.each do |person| %>
    <%= render "person_card", person: %>
  <% end %>
</div>
